server:
  port: ${PORT:8080}
  error:
    include-message: always
    include-binding-errors: always

spring:
  application:
    name: syncstream-backend
  
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/syncstream}
      auto-index-creation: true
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}
      password: ${SPRING_DATA_REDIS_PASSWORD:}

app:
  jwt:
    secret: ${JWT_SECRET:5573796e6353747265616d53757065725365637265744b65793230323623}
    expiration: ${JWT_EXPIRATION:86400000} # 24 hours in ms
  cors:
    allowed-origins: ${FRONTEND_URL:http://localhost:5173}

vapid:
  public.key: ${VAPID_PUBLIC_KEY:BO_qavSKoMxt4PGP-HDaS4rw5pTMhruCUZT-CcJtenirGQS2l2nSIOLEJwVcwIdTCGra4ugkkIq69ufbgG-o3e4}
  private.key: ${VAPID_PRIVATE_KEY:35Go-c4_DEazieW0GwTAt8wl-WkCkfsOFF19Tra56g4}
  subject: mailto:admin@syncstream.com

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env
  endpoint:
    health:
      show-details: always
